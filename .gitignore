# Dependencias
node_modules/
vendor/

# Builds
dist/
build/
/apps/web/dist/

# Entornos / secretos
.env
.env.local
.env.*.local
*.local

# Logs
*.log
npm-debug.log*

# Editor / SO
.DS_Store
.idea/
.vscode/
*.swp

# PHP / CI4
# El writable/ de CI4 lo gobierna apps/api/.gitignore: ignora los contenidos de
# runtime (cache/logs/session/uploads/debugbar) y conserva los index.html, que son
# necesarios para que exista el WRITEPATH en el checkout de CI.
phpstan.cache
.phpunit.cache/

# Fuente de datos con PII real (el dato vive en db.json, no se trackea el Excel)
CPJ_MEL_v1_9_seguimiento_actualizado*.xlsx
*.xlsx

# Python (extractor ETL en tools/)
__pycache__/
*.pyc
